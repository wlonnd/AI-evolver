# 路由表模板 (Route Table Template)

> **说明**：当本项目引入了新的长生命周期业务流或独立的子系统（如：新加了后台管理面板），必须复制本模板，重命名为 `路由表_XXX.md`，并在主 `架构地图.md` 中登记引用。

## 0. 模块说明
*   **模块名称**：XXX
*   **模块职责**：XXX

## 1. 核心意图与路由映射 (Intent Routing)
*(AI 必须通过匹配以下意图，找到对应的目标路径或操作脚本)*

| 意图分类 (Intent) | 目标文件/脚本路径 (Target) | 核心操作约束 (Constraint) |
| :--- | :--- | :--- |
| **【功能增强】** 修改登录验证逻辑 | `src/auth/login.js` | 必须同步修改对应的 JWT token 解析测试用例 |
| **【配置修改】** 调整环境变量 | `config/env.production` | 修改后必须走 `ops/deploy.sh` 重启服务 |
| **【报错排查】** 数据库连接池超时 | `src/db/connection.js` | 去经验库查历史，大概率是忘了 close() |

## 2. 自动化验证流 (Verification Flow)
*(修改完上述任何代码后，AI 必须执行以下哪种命令来验证自己没写坏)*
*   **单元测试命令**：`npm run test:xxx`
*   **本地启动命令**：`npm run dev`
