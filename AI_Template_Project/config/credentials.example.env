# 凭据模板文件 (Credentials Template)
# 严禁将真实的密码、Token 和私钥硬编码在 ops_scripts 或源码中。
# 使用前请将此文件复制为 credentials.env 并填入真实值，该文件已被 .gitignore 忽略。

# 示例：
DB_HOST=127.0.0.1
DB_PASS=your_password_here
API_TOKEN=your_token_here
